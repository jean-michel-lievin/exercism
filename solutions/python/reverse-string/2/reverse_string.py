"""
Reversing strings (reading them from right to left, 
rather than from left to right) is a 
surprisingly common task in programming.
"""
def reverse(text):
    """
    Reverse a given string.
    param text : str
    return str
    """
    return text[::-1]
